#include "main.h"
/**
 * _isdigit - function that checks for 1 number digits
 * @c: interger
 * Return: 0
 *
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}

